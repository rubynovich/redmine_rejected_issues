%p
  = content_tag(:label, l(:label_rejected_issue_status))
  = select_tag("settings[issue_status]", options_from_collection_for_select(IssueStatus.all, :id, :name, @settings[:issue_status].try(:to_i))
